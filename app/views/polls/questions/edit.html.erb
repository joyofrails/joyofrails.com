<%= render Pages::Header.new(title: "Edit Poll Question") %>
<div class="section-content container py-gap mb-3xl">
  <%= turbo_frame_tag dom_id(@question, :edit) do %>
    <%= render "form", poll: @poll, question: @question %>
  <% end %>

  <%= link_to sanitize("&larr; Back to poll"), poll_path(@poll) %>
</div>
