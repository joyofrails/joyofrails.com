<%= turbo_stream.update Polls::Question.new, "" %>

<%= turbo_stream.prepend "questions" do %>
  <%= render @question, poll: @poll %>
<% end %>

<%= turbo_stream.prepend "flash", partial: "application/flash" %>
