<%= turbo_frame_tag poll, class: "poll" do %>
  <div class="header">
    <h5 class="mb-2">
      <%= poll.title %>
    </h5>
  </div>

  <% if voted_on?(poll) %>
    <%= render Share::Polls::Results.new(poll) %>
  <% else %>
    <%= render Share::Polls::Ballot.new(poll) %>
  <% end %>
<% end %>
