<div class="highlight font-sans rounded-md px-4 py-6 sm:px-6 lg:px-8">
  <p class="text-sm font-sans font-medium leading-6 text-gray-400">Count</p>
  <p class="mt-2 flex font-sans items-baseline gap-x-2">
    <span class="text-4xl font-semibold tracking-tight text-white"><%= counter.count %></span>
  </p>
  <%= tag.div data: { controller: "analytics", analytics_event_value: "Examples Counter Click" }, class: "flex items-center gap-x-3 mt-3" do %>
    <%= button_to "Increment",
      examples_counters_path(counter: { count: counter.count + 1 }),
      method: :put,
      class: "button primary",
      form: { data: { action: "submit->analytics#send" } } %>
    <%= button_to "Decrement",
      examples_counters_path(counter: { count: counter.count - 1 }),
      method: :put,
      class: "button warn",
      form: { data: { action: "submit->analytics#send" } } %>
    <%= button_to "Reset",
      examples_counters_path,
      method: :delete,
      class: "button secondary",
      form: { data: { action: "submit->analytics#send" } } %>
  <% end %>
</div>
