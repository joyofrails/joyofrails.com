<%= render Pages::Header.new(title: "New Poll") %>
<div class="section-content container py-gap mb-3xl">
  <%= render "form", poll: @poll %>

  <%= link_to sanitize("&larr; Back to polls"), author_polls_path %>
</div>
