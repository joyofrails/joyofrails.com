<%= turbo_stream.remove @question %>

<%= turbo_stream.update dom_id(@poll, :total) do %>
  <%= render "polls/total", poll: @poll %>
<% end %>

<%= turbo_stream.prepend "flash", partial: "application/flash" %>
