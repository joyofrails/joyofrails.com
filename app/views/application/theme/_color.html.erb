<% if custom_color_scheme? %>
  <style>
    <%= render ColorSchemes::Css.new(color_scheme: find_color_scheme) %>
  </style>
<% else %>
  <style>
    <%= render ColorSchemes::Css.new(color_scheme: ColorScheme.cached_default) %>
  </style>
<% end %>
